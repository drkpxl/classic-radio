starlette
uvicorn
pyyaml
# TFT readout. Pillow renders the frame (cross-platform); Blinka + rgb-display
# drive the ST7789 and only work on the Pi — imported lazily, only when the
# display is enabled, so a Mac dev box never needs them at runtime.
Pillow>=10.1
adafruit-blinka
adafruit-circuitpython-rgb-display
# Bluetooth A2DP output: async D-Bus to BlueZ. Pure-Python (imports on the Mac too,
# where there's just no system bus), imported lazily only when bluetooth is enabled.
dbus-fast
